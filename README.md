# Cars Library API 🚗

Backend-сервис для библиотеки автомобильной литературы и схем. Позволяет пользователям загружать, хранить и получать книги, схемы автомобилей, а также оставлять комментарии к публикациям.

## ✨ Возможности

* **Загрузка файлов** книг и схем автомобилей с сохранением в Yandex Cloud Storage (S3)
* **Получение файлов** и метаданных по идентификатору
* **Комментирование** публикаций (книг и схем)
* **Базовая аутентификация** для защиты API
* **Логирование** операций через Zap (серверные логи) и RabbitMQ (логи действий пользователей)
* **Полная CRUD-поддержка** для всех сущностей

## 🏗 Технологический стек

* **Язык:** Go 1.23.2+
* **Фреймворк:** [Gin](https://github.com/gin-gonic/gin) - HTTP-фреймворк
* **База данных:** PostgreSQL - хранение метаданных
* **Хранилище:** Yandex Cloud Storage (S3-совместимое) через AWS SDK
* **Очередь сообщений:** RabbitMQ - логирование пользовательских действий
* **Логирование:** [Zap](https://github.com/uber-go/zap) - структурированное логирование
* **Аутентификация:** Basic Auth

## 📦 Быстрый старт

### Предварительные требования

* Go 1.23.2 или выше
* PostgreSQL 15+
* Yandex Cloud Storage (или любое S3-совместимое хранилище)
* RabbitMQ (опционально, для логирования действий)

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-username/cars-library.git
cd cars-library
```

2. Установите зависимости:
```bash
go mod download
```

3. Создайте файл конфигурации `.env` на основе примера:
```bash
cp .env.example .env
```

4. Отредактируйте `.env`, указав ваши настройки:
```env
# Сервер
SERVER_HOST=localhost
SERVER_PORT=8080

# База данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=cars_library

# Yandex Cloud Storage (S3)
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_S3_BUCKET=your_bucket_name
AWS_S3_ENDPOINT=https://storage.yandexcloud.net
AWS_REGION=ru-central1

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
RABBITMQ_QUEUE=user_actions_log

# Аутентификация
BASIC_AUTH_USER=admin
BASIC_AUTH_PASSWORD=secure_password
```

### Запуск

```bash
# Запуск в режиме разработки
go run cmd/app/main.go
```

Сервер будет доступен по адресу: `http://localhost:8080`

## 🚀 API Endpoints

Все запросы требуют Basic Auth. Используйте учетные данные из конфигурации.

### Книги (Books)

#### Загрузить книгу
```http
POST /book
Content-Type: multipart/form-data
Authorization: Basic {base64_auth}
```

**Параметры формы:**
- `title` (string) - название книги
- `car_model` (string) - модель автомобиля
- `category` (string) - категория
- `user_id` (string) - UUID пользователя
- `uploaded_by` (string) - имя загрузившего
- `file` (binary) - файл книги

#### Получить книгу
```http
GET /book?id={uuid}
Authorization: Basic {base64_auth}
```

#### Удалить книгу
```http
DELETE /book?id={uuid}
Authorization: Basic {base64_auth}
```

### Схемы (Drawings)

#### Загрузить схему
```http
POST /drawing
Content-Type: multipart/form-data
Authorization: Basic {base64_auth}
```

**Параметры формы:** аналогично книгам

#### Получить схему
```http
GET /drawing?id={uuid}
Authorization: Basic {base64_auth}
```

#### Удалить схему
```http
DELETE /drawing?id={uuid}
Authorization: Basic {base64_auth}
```

### Комментарии (Comments)

#### Создать комментарий
```http
POST /comment
Content-Type: application/json
Authorization: Basic {base64_auth}
```

```json
{
  "user_id": "uuid",
  "book_id": "uuid",
  "drawing_id": "uuid",
  "username": "string",
  "content": "string"
}
```

#### Удалить комментарий
```http
DELETE /comment?id={uuid}
Authorization: Basic {base64_auth}
```

## 📁 Структура проекта

```
backend/
├── cmd/app/main.go              # Точка входа приложения
├── internal/
│   ├── adapters/                # Адаптеры (контроллеры и репозитории)
│   ├── app/                     # Инициализация приложения
│   ├── apperror/                # Ошибки приложения
│   ├── controller/http/v1/      # HTTP-контроллеры
│   ├── domain/                  # Доменные сущности и use cases
│   └── repository/              # Репозитории для работы с данными
├── pkg/
│   ├── config/                  # Конфигурация приложения
│   ├── connectors/              # Подключения к внешним сервисам
│   │   ├── pgconnector/        # PostgreSQL
│   │   ├── rabbitconnector/    # RabbitMQ
│   │   └── s3connector/       # Yandex Cloud Storage
│   └── utils/                   # Вспомогательные утилиты
└── frontend/                    # (В разработке) Frontend часть
```

## 🔧 Разработка

### Сборка проекта

```bash
# Сборка бинарного файла
go build -o cars-library cmd/app/main.go

# Запуск собранного бинарника
./cars-library
```

### Запуск тестов

```bash
go test ./...
```

### Форматирование кода

```bash
go fmt ./...
```

### Проверка зависимостей

```bash
go mod tidy
```

## 📊 База данных

### Структура таблиц

```sql
-- Таблица схем автомобилей
CREATE TABLE drawings (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    title text,
    file_url text,
    car_model text,
    category text,
    user_id uuid,
    uploaded_by text
);

-- Таблица книг
CREATE TABLE books (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    title text,
    file_url text,
    car_model text,
    category text,
    user_id uuid,
    uploaded_by text
);

-- Таблица комментариев
CREATE TABLE comments (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id uuid,
    book_id uuid,
    drawing_id uuid,
    username text,
    content text,
    created_at text
);
```

## 🔐 Особенности реализации

### Логирование действий пользователей

Сервис использует RabbitMQ для асинхронного логирования действий пользователей. Например, при создании комментария отправляется сообщение:

```go
// Пример из кода
func (u Usecase) CreateComment(ctx context.Context, task *entities.NewComment) (entities.NewComment, error) {
    // ... логика создания комментария ...
    
    // Отправка сообщения в RabbitMQ
    if err := u.messageQueue.Publish(
        ctx,
        u.infoMessageQueue,
        []byte(fmt.Sprintf("Smbd create new comment: %v", task.CommID)),
    ); err != nil {
        // Обработка ошибки
    }
    
    return u.taskRepository.CreateComment(ctx, *task)
}
```

### Работа с файлами

1. Файлы загружаются через multipart/form-data
2. Сохраняются в Yandex Cloud Storage (S3)
3. URL файла сохраняется в базе данных
4. При получении данных - возвращается URL для скачивания

## 🤝 Для работодателя

Этот проект демонстрирует следующие навыки:

* Разработка RESTful API на Go
* Работа с PostgreSQL и миграциями
* Интеграция с облачными хранилищами (Yandex Cloud Storage)
* Использование очередей сообщений (RabbitMQ)
* Структурированное логирование (Zap)
* Чистая архитектура и разделение слоев
* Аутентификация и авторизация
* Обработка файловых загрузок

## 📝 Заметки

* Проект использует чистую архитектуру с разделением на слои
* Все зависимости инкапсулированы через интерфейсы
* Конфигурация вынесена в отдельный модуль
* Логирование разделено на серверное (Zap) и бизнес-события (RabbitMQ)
* Проект готов к масштабированию и добавлению новых функций

## 📄 Лицензия

Это учебный проект, созданный для демонстрации навыков. Не предназначен для коммерческого использования.
```

## Что стоит дополнительно сделать:

1. **Создать файл `.env.example`** с шаблоном конфигурации
2. **Добавить Makefile** для упрощения команд разработки:
```makefile
build:
    go build -o cars-library cmd/app/main.go

run:
    go run cmd/app/main.go

test:
    go test ./...

fmt:
    go fmt ./...

tidy:
    go mod tidy
```

3. **Добавить docker-compose.yml** для локальной разработки с PostgreSQL и RabbitMQ

Этот README профессионально представит ваш проект работодателю! Он показывает, что вы умеете не только писать код, но и документировать его.